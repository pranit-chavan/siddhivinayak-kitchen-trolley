{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && export PRISMA_BINARIES_CACHE_DIR=/tmp/prisma-cache && export PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1 && npm install && npx prisma generate --schema=../database/schema.prisma && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && sh start.sh",
    "restartPolicyType": "ON_FAILURE"
  }
}
