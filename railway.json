{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npx prisma generate --schema=../database/schema.prisma && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && sh start.sh",
    "restartPolicyType": "ON_FAILURE"
  }
}
