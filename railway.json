{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate --schema=./database/schema.prisma && npm run build --workspace=backend"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma migrate deploy --schema=../database/schema.prisma && node dist/main",
    "restartPolicyType": "ON_FAILURE"
  }
}
